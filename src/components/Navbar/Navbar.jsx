import React from "react";

import "./Navbar.scss";

const Navbar = () => {
  return (
    <nav>
      <div>
        <img src={image.logo} alt="logo"></img>
      </div>
    </nav>
  );
};

export default Navbar;
